# packet-sniffer
A Python-based packet sniffer that captures live network traffic and displays packet details including source and destination IP addresses, protocol type, timestamp, and packet size using Scapy.
